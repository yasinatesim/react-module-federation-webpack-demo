import { ConsumeOptions } from "@module-federation/sdk";
import { Compilation } from "webpack";

//#region src/lib/sharing/resolveMatchedConfigs.d.ts
interface MatchedConfigs<T> {
  resolved: Map<string, T>;
  unresolved: Map<string, T>;
  prefixed: Map<string, T>;
}
declare function resolveMatchedConfigs<T extends ConsumeOptions>(compilation: Compilation, configs: [string, T][]): Promise<MatchedConfigs<T>>;
//#endregion
export { resolveMatchedConfigs };
//# sourceMappingURL=resolveMatchedConfigs.d.ts.map